{
  "name": "taxzilla-cli-workspace",
  "version": "0.2.0",
  "private": true,
  "packageManager": "bun@1.2.12",
  "engines": {
    "bun": ">=1.2.12"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "nx run-many -t build --all",
    "typecheck": "nx run-many -t typecheck --all",
    "lint": "nx run-many -t lint --all",
    "format": "nx run-many -t format --all",
    "dev": "bun run --cwd apps/cli dev",
    "cli": "bun run --cwd apps/cli src/index.tsx",
    "graph": "nx graph",
    "test": "vitest run",
    "test:coverage": "bun run --cwd packages/tax-engine test:coverage",
    "pack:tax-engine": "npm pack --dry-run --workspace packages/tax-engine",
    "pack:cli": "npm pack --dry-run --workspace apps/cli",
    "release:check": "bun run lint && bun run typecheck && bun run test && bun run pack:tax-engine && bun run pack:cli"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "@types/react": "^19.2.2",
    "@vitest/coverage-v8": "3.2.4",
    "nx": "22.5.2",
    "oxfmt": "^0.35.0",
    "oxlint": "^1.50.0",
    "typescript": "5.9.3",
    "vitest": "3.2.4"
  }
}
